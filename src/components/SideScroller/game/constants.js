export const TILE_SIZE = 40;
export const NUM_TILES = 16;
export const MOVE_SPEED = 2;
export const JUMP_SPEED = 10;
export const GRAVITY = 0.5;
export const HUD_HEIGHT = 80;

export const npcPosition = { x: 10, y: 0 };
export const TERMINAL_MESSAGE = "Hello, world! (Seems like every program starts with this.)";
